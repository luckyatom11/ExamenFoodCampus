Al terminar cada promp notifica al usuario si esta bien o necesecita generar alguna modificación al codigo generado

promp3:
Ubicación de salida: /src/Domain 
crea la clase 'Restaurante' con las siguientes propiedades:'Id', 'Nombre', 'Especialidad', 'HorarioApertura', 'HorarioCierre', posteriormente implementa una una extensión member llamado "EstaAbierto" en el que compare la hora actual con la hora de apertura con la hora de cierre.
Nota: Asegura que las propiedades de cadena no sean nulas o usa 'required' para evitar advertencias de compilación (CS8618).

promp4:
Ubicación de salida: /src/Domain (si no existe la carpeta, crea una nueva)
crea la clase 'DetallePedido' con propiedades: 'IdDetalle', 'IdPedido', 'IdPlatillo', 'Cantidad' y 'Subtotal'. 
Nota: Usa 'required' o inicializa las propiedades para evitar advertencias de nulabilidad (CS8618).

Restriccion: 
En DetallePedido, usa la nueva palabra clave field en el setter de Cantidad para validar que el valor sea mayor a 0, lanzando una excepción si es inválido, sin declarar un campo de respaldo manual.

promp5: 
Ubicación de salida: /src/Domain (si no existe la carpeta, crea una nueva)
Crea la clase 'Pedido' con las siguientes propiedades:'IdPedido', 'IdUsuario', 'IdRestaurante', 'FechaHora', 'CostoEnvio'
Nota: Inicializa las propiedades o usa 'required' para evitar advertencias.

notas: 
- Agrega una propiedad List<DetallePedido> Detalles que se inicialice como una lista vacía.
- Agrega una propiedad calculada (solo get) llamada Total que sume los Subtotal de todos los elementos en la lista Detalles más el CostoEnvio."

promp 6:
Crea una clase llamada DapperContext.
1. Debe recibir IConfiguration a través del constructor para leer la cadena de conexión llamada 'SomeeConnection'.
2. Implementa un método llamado CrearConexion() que devuelva una nueva instancia de SqlConnection.
3. Asegúrate de importar los namespaces necesarios de Microsoft.Data.SqlClient y System.Data.
4. Indica explícitamente que se deben ejecutar los siguientes comandos para instalar las dependencias:
   - `dotnet add package Microsoft.Data.SqlClient`
   - `dotnet add package Microsoft.Extensions.Configuration.Abstractions`

promo 7:
Crea un archivo appsettings.json que contenga la sección ConnectionStrings.
workstation id=test_UTM_ABCO.mssql.somee.com;packet size=4096;user id=UTM_ABCO;pwd=Acoba1120;data source=test_UTM_ABCO.mssql.somee.com;persist security info=False;initial catalog=test_UTM_ABCO;TrustServerCertificate=True